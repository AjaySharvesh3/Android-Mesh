<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ebook.kindle.androidmesh">

    <!--TODO #2: Permission to use Internet -->


    <!--TODO #3: Create Java Class (Model Class) and name it as PlayerModel, then (Follow Book)  -->

    <!--TODO #4: Create Proper Layout for RecyclerView, right click on res -> layout directory and select New -> Layout Resource File and name the file as player_item, than (Follow Book) -->

    <!--TODO #5: Create Java Class (Adapter Class) and name it as PlayerListAdapter, then (Follow Book) -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".PlayerListActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>